import React from 'react';
import ReactDOM from 'react-dom';
import {App} from './App'
//nos va a permitir rendirizar todos los metodos
//le pasamos(lo que queremos hacer, donde)
ReactDOM.render(<App/>, document.getElementById('root'))